'use strict'

var fecha = new Date();

var year = fecha.getFullYear();
var mes = fecha.getMonth();
var dia = fecha.getDate();
var hora = fecha.getHours();
var minutos = fecha.getMinutes();
var segundos = fecha.getSeconds();

var textoHora = `
    El año es: ${year}
    El mes es: ${mes}
    El dia es: ${dia}
    El hora es: ${hora}
    Los minutos son: ${minutos}
    Los segundo son: ${segundos}
`;

console.log(textoHora);

//Funciones Matematicas de JS
// Google: Javascript Math
// Math es numero aliatorio
// Math.celi es para numeros enteros sin decimales
//console.log(Math.ceil(Math.random()*10000));
//console.log(Math.ceil(Math.random()*10000));
//console.log(Math.ceil(Math.random()*10000));
console.log(Math.ceil(Math.random()*10000));

// vamos en proceso
// sisiis
// yes
// si 


/// vamo s

// sius
//15 champions

///siiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiii
